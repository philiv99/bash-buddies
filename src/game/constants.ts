/**
 * Bash Buddies — Game Constants
 */

/** Number of friends needed to win */
export const FRIENDS_TO_WIN = 3;

/** Points awarded per friendship */
export const FRIENDSHIP_BONUS = 100;

/** Max conversation topics per guest */
export const MAX_TOPICS_PER_CONVERSATION = 3;

/** Number of guests to show at the party */
export const PARTY_SIZE = 8;

/** localStorage prefix */
export const STORAGE_PREFIX = 'bash-buddies:';

/** High score storage key */
export const HIGH_SCORE_KEY = `${STORAGE_PREFIX}highScore`;
